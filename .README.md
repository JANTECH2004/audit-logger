#Simple Audit Logger System
How to use =>
clone the repo using "git clone https://github.com/JANTECH2004/audit-logger.git"
after clone install node into your system and run the file "index.js" by using command "node index.js"